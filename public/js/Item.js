//Class Item build stuff to be set in LS
class Item {
    constructor(id, quantity, varnish) {
        this.id = id;
        this.quantity = parseInt(quantity);
        this.varnish = varnish;
    }
}
